Сюда положи свой конфиг Nginx (например `nginx.conf`).

Пример задачи: раздавать статику из `../frontend` и проксировать запросы `/api` на `http://backend:8000`. В `docker-compose` добавить сервис `nginx`, который монтирует этот конфиг и папку frontend.
