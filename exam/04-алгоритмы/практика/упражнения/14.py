"""
Задание: Прыжки по массиву
Массив неотрицательных чисел: nums[i] — макс. длина прыжка с позиции i.
Определи, можно ли дойти от индекса 0 до последнего.
Пример: [2, 3, 1, 1, 4] → True, [3, 2, 1, 0, 4] → False.

Совет: храни «достижимый максимум» (дальний индекс). На каждом шаге обновляй его
как max(max_reach, i + nums[i]). Если i > max_reach — вернуть False.
"""
