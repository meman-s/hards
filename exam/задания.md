# Тестовые задания

## 1. Обход неориентированного графа в ширину

### Задание 1.1

Дан неориентированный граф в виде списка смежности. Реализуйте функцию `bfs_undirected_1`, которая выполняет обход графа в ширину, начиная с заданной вершины, и возвращает список вершин в порядке их посещения.

**Входные данные:**
- `graph`: список списков, где `graph[i]` содержит номера вершин, смежных с вершиной `i`
- `start`: номер стартовой вершины (0 <= start < n)
- `n`: количество вершин (опционально, по умолчанию `len(graph)`)

**Выходные данные:**
- Список вершин в порядке их посещения при обходе в ширину

**Пример:**
```python
graph = [[1, 2], [0, 3], [0, 4], [1], [2]]
start = 0
# Ожидаемый результат: [0, 1, 2, 3, 4]
```

---

### Задание 1.2

Дан неориентированный граф в виде списка смежности. Реализуйте функцию `bfs_undirected_2`, которая находит расстояние от стартовой вершины до всех остальных вершин, используя обход в ширину. Если вершина недостижима, расстояние должно быть -1.

**Входные данные:**
- `graph`: список списков, где `graph[i]` содержит номера вершин, смежных с вершиной `i`
- `start`: номер стартовой вершины (0 <= start < n)
- `n`: количество вершин (опционально, по умолчанию `len(graph)`)

**Выходные данные:**
- Список расстояний от `start` до каждой вершины (индекс списка соответствует номеру вершины)

**Пример:**
```python
graph = [[1, 2], [0, 3], [0, 4], [1], [2]]
start = 0
# Ожидаемый результат: [0, 1, 1, 2, 2]
```

---

## 2. Обход неориентированного графа в глубину

### Задание 2.1

Дан неориентированный граф в виде списка смежности. Реализуйте функцию `dfs_undirected_1`, которая выполняет обход графа в глубину, начиная с заданной вершины, и возвращает список вершин в порядке их посещения.

**Входные данные:**
- `graph`: список списков, где `graph[i]` содержит номера вершин, смежных с вершиной `i`
- `start`: номер стартовой вершины (0 <= start < n)
- `n`: количество вершин (опционально, по умолчанию `len(graph)`)

**Выходные данные:**
- Список вершин в порядке их посещения при обходе в глубину

**Пример:**
```python
graph = [[1, 2], [0, 3], [0, 4], [1], [2]]
start = 0
# Ожидаемый результат: [0, 1, 3, 2, 4] (один из возможных вариантов)
```

---

### Задание 2.2

Дан неориентированный граф в виде списка смежности. Реализуйте функцию `dfs_undirected_2`, которая проверяет, является ли граф связным (все вершины достижимы друг из друга).

**Входные данные:**
- `graph`: список списков, где `graph[i]` содержит номера вершин, смежных с вершиной `i`
- `n`: количество вершин (опционально, по умолчанию `len(graph)`)

**Выходные данные:**
- `True`, если граф связный, иначе `False`

**Пример:**
```python
graph = [[1, 2], [0, 3], [0, 4], [1], [2]]
# Ожидаемый результат: True

graph = [[1], [0], [3], [2], []]
# Ожидаемый результат: False
```

---

## 3. Обход ориентированного графа в ширину

### Задание 3.1

Дан ориентированный граф в виде списка смежности. Реализуйте функцию `bfs_directed_1`, которая выполняет обход графа в ширину, начиная с заданной вершины, и возвращает список вершин в порядке их посещения.

**Входные данные:**
- `graph`: список списков, где `graph[i]` содержит номера вершин, в которые ведут рёбра из вершины `i`
- `start`: номер стартовой вершины (0 <= start < n)
- `n`: количество вершин (опционально, по умолчанию `len(graph)`)

**Выходные данные:**
- Список вершин в порядке их посещения при обходе в ширину

**Пример:**
```python
graph = [[1, 2], [3], [4], [], []]
start = 0
# Ожидаемый результат: [0, 1, 2, 3, 4]
```

---

### Задание 3.2

Дан ориентированный граф в виде списка смежности. Реализуйте функцию `bfs_directed_2`, которая находит все вершины, достижимые из заданной стартовой вершины, используя обход в ширину.

**Входные данные:**
- `graph`: список списков, где `graph[i]` содержит номера вершин, в которые ведут рёбра из вершины `i`
- `start`: номер стартовой вершины (0 <= start < n)
- `n`: количество вершин (опционально, по умолчанию `len(graph)`)

**Выходные данные:**
- Множество (set) вершин, достижимых из `start`

**Пример:**
```python
graph = [[1, 2], [3], [4], [], []]
start = 0
# Ожидаемый результат: {0, 1, 2, 3, 4}

graph = [[1], [2], [0], []]
start = 3
# Ожидаемый результат: {3}
```

---

## 4. Обход ориентированного графа в глубину

### Задание 4.1

Дан ориентированный граф в виде списка смежности. Реализуйте функцию `dfs_directed_1`, которая выполняет обход графа в глубину, начиная с заданной вершины, и возвращает список вершин в порядке их посещения.

**Входные данные:**
- `graph`: список списков, где `graph[i]` содержит номера вершин, в которые ведут рёбра из вершины `i`
- `start`: номер стартовой вершины (0 <= start < n)
- `n`: количество вершин (опционально, по умолчанию `len(graph)`)

**Выходные данные:**
- Список вершин в порядке их посещения при обходе в глубину

**Пример:**
```python
graph = [[1, 2], [3], [4], [], []]
start = 0
# Ожидаемый результат: [0, 1, 3, 2, 4] (один из возможных вариантов)
```

---

### Задание 4.2

Дан ориентированный граф в виде списка смежности. Реализуйте функцию `dfs_directed_2`, которая находит все сильно связные компоненты графа, используя алгоритм Косарайю (Kosaraju) или Тарьяна (Tarjan).

**Входные данные:**
- `graph`: список списков, где `graph[i]` содержит номера вершин, в которые ведут рёбра из вершины `i`
- `n`: количество вершин (опционально, по умолчанию `len(graph)`)

**Выходные данные:**
- Список списков, где каждый внутренний список содержит вершины одной сильно связной компоненты

**Пример:**
```python
graph = [[1], [2], [0], [4], [3]]
n = 5
# Ожидаемый результат: [[0, 1, 2], [3, 4]]
```

---

## 5. Топологическая сортировка

### Задание 5.1

Дан ориентированный ациклический граф (DAG) в виде списка смежности. Реализуйте функцию `topological_sort_1`, которая выполняет топологическую сортировку вершин графа.

**Входные данные:**
- `graph`: список списков, где `graph[i]` содержит номера вершин, в которые ведут рёбра из вершины `i`
- `n`: количество вершин

**Выходные данные:**
- Список вершин в порядке топологической сортировки, или `None`, если граф содержит цикл

**Пример:**
```python
graph = [[], [0], [0], [1, 2], [1, 3]]
n = 5
# Ожидаемый результат: [4, 3, 2, 1, 0] (один из возможных вариантов)
```

---

### Задание 5.2

Дан ориентированный граф в виде списка смежности. Реализуйте функцию `topological_sort_2`, которая находит все возможные топологические сортировки графа (если граф ациклический) или возвращает пустой список, если граф содержит цикл.

**Входные данные:**
- `graph`: список списков, где `graph[i]` содержит номера вершин, в которые ведут рёбра из вершины `i`
- `n`: количество вершин

**Выходные данные:**
- Список всех возможных топологических сортировок (каждая сортировка - это список вершин), или пустой список, если граф содержит цикл

**Пример:**
```python
graph = [[], [0], []]
n = 3
# Ожидаемый результат: [[1, 2, 0], [2, 1, 0]] (два возможных варианта)
```

---

## 6. Профилирование и оптимизация кода

### Задание 6.1

Дан следующий код на Python:

```python
def process_data(data):
    result = []
    for item in data:
        if item > 0:
            result.append(item * 2)
    return result

def main():
    data = list(range(1000000))
    result = process_data(data)
    return sum(result)
```

Найдите узкое место в этом коде, используя профилирование (cProfile или line_profiler), и оптимизируйте код. Напишите оптимизированную версию функции `process_data_optimized`.

**Требования:**
- Используйте профилирование для выявления узкого места
- Предоставьте оптимизированную версию кода
- Объясните, какие изменения были внесены и почему

---

### Задание 6.2

Дан следующий код на Python:

```python
def find_duplicates(items):
    duplicates = []
    for i in range(len(items)):
        for j in range(i + 1, len(items)):
            if items[i] == items[j]:
                duplicates.append(items[i])
    return duplicates

def process_large_dataset(data):
    result = []
    for item in data:
        if item not in result:
            result.append(item)
    return result
```

Найдите узкие места в этом коде, используя профилирование, и оптимизируйте обе функции. Напишите оптимизированные версии `find_duplicates_optimized` и `process_large_dataset_optimized`.

**Требования:**
- Используйте профилирование для выявления узких мест
- Предоставьте оптимизированные версии функций
- Объясните, какие изменения были внесены и почему

---

## 7. Nginx: reverse proxy, load balancing

### Задание 7.1

Настройте Nginx в качестве reverse proxy для двух backend-серверов:
- Backend 1: `http://localhost:8001`
- Backend 2: `http://localhost:8002`

Nginx должен работать на порту 80 и проксировать запросы на эти серверы. Напишите конфигурацию Nginx.

**Требования:**
- Nginx слушает на порту 80
- Запросы проксируются на backend-серверы
- Используйте директиву `proxy_pass`
- Добавьте заголовки для передачи информации о клиенте

---

### Задание 7.2

Настройте Nginx для load balancing между тремя backend-серверами:
- Backend 1: `http://localhost:8001`
- Backend 2: `http://localhost:8002`
- Backend 3: `http://localhost:8003`

Используйте алгоритм round-robin с весами: сервер 1 - вес 3, сервер 2 - вес 2, сервер 3 - вес 1. Напишите конфигурацию Nginx.

**Требования:**
- Используйте upstream блок для определения серверов
- Примените алгоритм weighted round-robin
- Nginx слушает на порту 80
- Добавьте health checks (опционально)

---

## 8. SQL: Нормализация, Подзапросы, Оконные функции, Индексы, Триггеры, Оптимизация запросов, EXPLAIN ANALYZE

### Задание 8.1

Дана следующая таблица `orders`:

```sql
CREATE TABLE orders (
    id SERIAL PRIMARY KEY,
    customer_id INTEGER,
    customer_name VARCHAR(100),
    customer_email VARCHAR(100),
    product_id INTEGER,
    product_name VARCHAR(100),
    product_price DECIMAL(10, 2),
    quantity INTEGER,
    order_date DATE,
    total_amount DECIMAL(10, 2)
);
```

**Задачи:**
1. Нормализуйте таблицу до 3NF (создайте необходимые таблицы)
2. Напишите запрос с использованием подзапроса для получения всех заказов клиентов, у которых общая сумма заказов превышает 1000
3. Напишите запрос с оконной функцией для получения ранга клиентов по общей сумме заказов
4. Создайте индексы для оптимизации запросов
5. Используйте EXPLAIN ANALYZE для проверки производительности запроса

---

### Задание 8.2

Дана следующая схема базы данных:

```sql
CREATE TABLE employees (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100),
    department_id INTEGER,
    salary DECIMAL(10, 2),
    hire_date DATE
);

CREATE TABLE departments (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100)
);
```

**Задачи:**
1. Напишите запрос с использованием подзапроса для получения сотрудников, чья зарплата выше средней зарплаты в их отделе
2. Напишите запрос с оконной функцией для получения накопительной суммы зарплат по дате найма в каждом отделе
3. Создайте составной индекс для оптимизации запросов по `department_id` и `salary`
4. Создайте триггер, который автоматически обновляет статистику отдела при добавлении нового сотрудника
5. Используйте EXPLAIN ANALYZE для анализа производительности запроса с оконной функцией

---

## 9. MongoDB: Запросы, агрегации, Join ($lookup), Индексы (compound, partial, text)

### Задание 9.1

Дана коллекция `users` со следующими документами:

```json
{
  "_id": ObjectId("..."),
  "name": "John Doe",
  "email": "john@example.com",
  "age": 30,
  "city": "Moscow",
  "tags": ["developer", "python"]
}
```

И коллекция `orders`:

```json
{
  "_id": ObjectId("..."),
  "user_id": ObjectId("..."),
  "product": "Laptop",
  "price": 1000,
  "date": ISODate("2024-01-15")
}
```

**Задачи:**
1. Напишите запрос для поиска всех пользователей старше 25 лет из Москвы
2. Напишите агрегацию для получения общей суммы заказов каждого пользователя
3. Используйте `$lookup` для объединения коллекций `users` и `orders`
4. Создайте составной индекс на полях `age` и `city`
5. Создайте частичный индекс (partial index) для пользователей старше 18 лет
6. Создайте текстовый индекс на поле `tags` для полнотекстового поиска

---

### Задание 9.2

Дана коллекция `products`:

```json
{
  "_id": ObjectId("..."),
  "name": "Smartphone",
  "category": "Electronics",
  "price": 500,
  "stock": 100,
  "description": "Latest smartphone with advanced features"
}
```

И коллекция `reviews`:

```json
{
  "_id": ObjectId("..."),
  "product_id": ObjectId("..."),
  "user_id": ObjectId("..."),
  "rating": 5,
  "comment": "Great product!"
}
```

**Задачи:**
1. Напишите запрос для поиска всех продуктов категории "Electronics" с ценой меньше 1000
2. Напишите агрегацию для получения среднего рейтинга каждого продукта
3. Используйте `$lookup` для объединения `products` и `reviews` с фильтрацией по рейтингу >= 4
4. Создайте составной индекс на полях `category` и `price`
5. Создайте частичный индекс для продуктов со складом больше 0
6. Создайте текстовый индекс на поле `description` для полнотекстового поиска

---

## 10. Pytest: fixtures, parametrize, markers

### Задание 10.1

Дан следующий код:

```python
class Database:
    def __init__(self):
        self.data = {}
    
    def insert(self, key, value):
        self.data[key] = value
    
    def get(self, key):
        return self.data.get(key)
    
    def delete(self, key):
        if key in self.data:
            del self.data[key]
            return True
        return False

def process_data(db, items):
    result = []
    for item in items:
        db.insert(item, item * 2)
        result.append(db.get(item))
    return result
```

Напишите тесты с использованием:
1. Фикстуры для создания и очистки базы данных перед/после каждого теста
2. Параметризации для тестирования функции `process_data` с разными наборами данных
3. Маркеров для категоризации тестов (например, `@pytest.mark.slow`, `@pytest.mark.integration`)

---

### Задание 10.2

Дан следующий код:

```python
def validate_email(email):
    if not email or '@' not in email:
        return False
    parts = email.split('@')
    if len(parts) != 2:
        return False
    local, domain = parts
    if not local or not domain:
        return False
    if '.' not in domain:
        return False
    return True

def calculate_total(prices, discount=0):
    if not prices:
        return 0
    total = sum(prices)
    if discount > 0:
        total = total * (1 - discount / 100)
    return total
```

Напишите тесты с использованием:
1. Фикстуры для подготовки тестовых данных
2. Параметризации для тестирования `validate_email` с различными входными данными (валидные и невалидные email)
3. Параметризации для тестирования `calculate_total` с различными комбинациями цен и скидок
4. Маркеров для разделения unit-тестов и integration-тестов

---

## 11. Юнит тесты

### Задание 11.1

Дан следующий код:

```python
class Calculator:
    def add(self, a, b):
        return a + b
    
    def subtract(self, a, b):
        return a - b
    
    def multiply(self, a, b):
        return a * b
    
    def divide(self, a, b):
        if b == 0:
            raise ValueError("Division by zero")
        return a / b
    
    def power(self, a, b):
        return a ** b
```

Напишите полный набор юнит-тестов для класса `Calculator`, покрывающий:
- Все методы класса
- Граничные случаи (деление на ноль, отрицательные числа, ноль)
- Различные типы входных данных (целые числа, числа с плавающей точкой)

---

### Задание 11.2

Дан следующий код:

```python
class UserManager:
    def __init__(self):
        self.users = {}
    
    def add_user(self, user_id, name, email):
        if user_id in self.users:
            raise ValueError(f"User {user_id} already exists")
        if not name or not email:
            raise ValueError("Name and email are required")
        if '@' not in email:
            raise ValueError("Invalid email format")
        self.users[user_id] = {'name': name, 'email': email}
    
    def get_user(self, user_id):
        if user_id not in self.users:
            raise KeyError(f"User {user_id} not found")
        return self.users[user_id]
    
    def delete_user(self, user_id):
        if user_id not in self.users:
            raise KeyError(f"User {user_id} not found")
        del self.users[user_id]
    
    def list_users(self):
        return list(self.users.keys())
```

Напишите полный набор юнит-тестов для класса `UserManager`, покрывающий:
- Все методы класса
- Валидацию входных данных
- Обработку ошибок (исключения)
- Граничные случаи (пустые значения, несуществующие пользователи)

---

## 12. Docker: multi-stage builds, secrets, ssh mount

### Задание 12.1

Дан следующий Dockerfile:

```dockerfile
FROM python:3.9
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
CMD ["python", "app.py"]
```

Оптимизируйте Dockerfile, используя multi-stage build:
1. Первый stage: сборка приложения и установка зависимостей
2. Второй stage: финальный образ только с необходимыми файлами
3. Минимизируйте размер итогового образа
4. Используйте секреты для безопасной передачи чувствительных данных (например, API ключей) во время сборки

---

### Задание 12.2

Дан следующий Dockerfile:

```dockerfile
FROM node:16
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build
CMD ["node", "dist/index.js"]
```

Оптимизируйте Dockerfile:
1. Используйте multi-stage build для разделения этапов сборки и выполнения
2. Настройте монтирование SSH ключей для доступа к приватным репозиториям во время `npm install`
3. Используйте секреты для передачи токенов доступа
4. Минимизируйте количество слоев и размер образа
5. Добавьте healthcheck для проверки работоспособности контейнера

---

## 13. Docker Compose: healthcheck, services, networks, volumes

### Задание 13.1

Создайте `docker-compose.yml` для следующего приложения:
- Web-сервер (Python Flask) на порту 5000
- База данных PostgreSQL
- Redis для кэширования

**Требования:**
1. Настройте healthcheck для всех сервисов
2. Настройте сети: web-сервер и Redis в одной сети, PostgreSQL в отдельной сети
3. Настройте volumes для персистентности данных PostgreSQL
4. Настройте зависимости между сервисами (web зависит от db и redis)
5. Используйте переменные окружения для конфигурации

---

### Задание 13.2

Создайте `docker-compose.yml` для микросервисной архитектуры:
- API Gateway (Nginx) на порту 80
- Backend Service 1 (Python) на порту 8001
- Backend Service 2 (Node.js) на порту 8002
- База данных MongoDB
- База данных PostgreSQL

**Требования:**
1. Настройте healthcheck для всех сервисов
2. Создайте отдельные сети: frontend сеть (API Gateway), backend сеть (сервисы), database сеть (БД)
3. Настройте volumes для персистентности данных обеих БД
4. Настройте зависимости: API Gateway зависит от обоих backend-сервисов, сервисы зависят от соответствующих БД
5. Используйте переменные окружения для конфигурации
6. Настройте restart policies для всех сервисов
